[build]
  base = "frontend"
  command = "npm install --legacy-peer-deps --force && npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18.19.0"
  NPM_VERSION  = "9.9.0"
  CI           = "false"

[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
