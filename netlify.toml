[build]
  base = "frontend"
  command = "npm install --legacy-peer-deps --force && rm -rf node_modules/schema-utils/node_modules && npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20.11.0"
  NPM_VERSION  = "10.2.4"
  CI           = "false"

[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
