[build]
  base = "frontend"
  command = "npm install --legacy-peer-deps --force && npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20.11.0"
  NPM_VERSION  = "10.2.3"
  CI           = "false"

[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
